module nimble_xchain::errors {
    const ERR_NOT_ALLOWED: u64 = 200;
    const ERR_INVALID_CHAIN_ID: u64 = 201;
    const ERR_INVALID_EMITTER_ADDRESS: u64 = 202;
    const ERR_INVALID_FEE_CONFIG: u64 = 203;
}
